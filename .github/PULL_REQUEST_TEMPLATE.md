## 摘要

一两句话说明这个 PR 做了什么、为什么需要。

## 背景与动机

- 要解决的问题或要满足的需求
- 相关的 issue 链接（如有）

## 改动清单

### 后端（Rust）

| 文件 | 改动 |
|---|---|
| `src-tauri/src/...` | 说明 |

### 前端（TypeScript/React）

| 文件 | 改动 |
|---|---|
| `src/...` | 说明 |

### 配套

| 文件 | 改动 |
|---|---|
| `README.md` / `docs/...` | 说明 |

> 与主题无关的改动（格式、lint、重构）请单独提交并说明原因，避免混入功能改动。

## 测试

- [ ] `pnpm check` 全绿（format / lint / version:check / build / rust fmt / clippy / cargo test）
- [ ] 新增/更新的单元测试已列出（名称与覆盖场景）
- [ ] 如涉及真实数据验证（如 MPCORB 全量导入），说明数据来源、规模与验证步骤

## 平台说明

- 改动涉及平台相关行为时，说明已验证的平台（Windows / Linux / macOS）
- 数据路径、文件对话框等是否依赖特定平台
